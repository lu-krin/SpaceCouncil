# SpaceCouncil

SpaceCouncil is an aerospace multi-agent council module for cross-discipline mission decisions, trade studies, and risk posture calls.

It combines:
- A council skill: [`SpaceCouncil`](skills/SpaceCouncil/SKILL.md)
- Seven specialist agents in [`agents/`](agents)
- Multi-provider routing and model/tool defaults in [`defaults.yaml`](defaults.yaml)

## What It Does

SpaceCouncil runs a structured decision workflow:
1. Discovers and includes all available project agents.
2. Performs mandatory documentation review before debate.
3. Falls back to ECSS references from `https://www.ecss.nl` when local standards coverage is missing.
4. Runs a 3-round council debate (initial positions, challenges, convergence).
5. Produces one final verdict (`Proceed`, `Proceed with Conditions`, `Block`, or a direct answer for non-go/no-go prompts).

## Agent Roster

- `HardwareEngineer`: hardware architecture, qualification, flight readiness
- `SoftwareEngineer`: flight/ground software reliability and compliance
- `PrincipalInvestigator`: science value, mission objective tradeoffs
- `ProductAssurance`: QA, compliance evidence, nonconformance governance
- `SafetySecurity`: ISS safety/security, THL/BSL, vulnerability posture
- `ESARep`: gate readiness, ECSS/ESA evidence discipline
- `TestManager`: test strategy, GSE readiness, verification campaign control

## Repository Layout

```text
SpaceCouncil/
├── agents/                         # Canon agent definitions
├── skills/SpaceCouncil/            # Canon skill definition + sidecar
├── defaults.yaml                   # Provider + model/tool + skill routing
├── .claude-plugin/plugin.json      # Claude plugin metadata
├── .claude/                        # Provider-installed artifacts
├── .codex/                         # Provider-installed artifacts
├── .gemini/                        # Provider-installed artifacts
└── .opencode/                      # Provider-installed artifacts
```

## Installation

### Prerequisites

- `git` installed
- One supported tool installed and authenticated:
  - Anthropic Claude
  - ChatGPT Codex
  - Google Gemini

### Clone on Linux

```bash
git clone <YOUR_GITHUB_REPO_URL>
cd SpaceCouncil
```

### Clone on macOS

```bash
git clone <YOUR_GITHUB_REPO_URL>
cd SpaceCouncil
```

### Clone on Windows (PowerShell)

```powershell
git clone <YOUR_GITHUB_REPO_URL>
Set-Location SpaceCouncil
```

## Tool Setup

### Anthropic Claude

1. Copy `.claude/` and `.claude-plugin/` into your target project root.
2. Copy `skills/SpaceCouncil/` into your target project at `skills/SpaceCouncil/`.
3. Confirm `.claude-plugin/plugin.json` includes `"skills": ["./skills"]`.
4. Start Claude in the project and invoke `SpaceCouncil` (for example: `/SpaceCouncil <topic>`).

### ChatGPT Codex

1. Copy `.codex/` into your target project root.
2. If the target project already has `.codex/config.toml`, merge the `forge-council agents` block instead of overwriting the whole file.
3. Confirm these files exist:
   - `.codex/config.toml`
   - `.codex/agents/*.toml`
   - `.codex/skills/SpaceCouncil/SKILL.md`
4. Start Codex in the project and run a SpaceCouncil prompt.

### Google Gemini

1. Copy `.gemini/` into your target project root.
2. Copy `skills/SpaceCouncil/SKILL.md` into your target project at `skills/SpaceCouncil/SKILL.md`.
3. Keep `defaults.yaml` entry `skills.gemini.SpaceCouncil` enabled.
4. Start Gemini in the project and run a SpaceCouncil prompt.

## Usage

Use your client's skill invocation mechanism and call `SpaceCouncil` with a decision question.

Example prompts:
- `Run SpaceCouncil for a go/no-go recommendation on entering CDR.`
- `Use SpaceCouncil to evaluate avionics architecture option A vs B under ECSS constraints.`
- `Run SpaceCouncil on mission readiness risk posture and provide a verdict with conditions.`

If your client supports slash-style skill invocation, use:

```text
/SpaceCouncil <topic or decision question>
```

## Configuration Notes

- Canon sources to edit:
  - [`agents/*.md`](agents)
  - [`skills/SpaceCouncil/SKILL.md`](skills/SpaceCouncil/SKILL.md)
  - [`skills/SpaceCouncil/SKILL.yaml`](skills/SpaceCouncil/SKILL.yaml)
  - [`defaults.yaml`](defaults.yaml)
- Provider folders (`.claude/`, `.codex/`, `.gemini/`, `.opencode/`) are install outputs and may be regenerated by your toolchain.

## Scope and Assumptions

- Designed for aerospace programs requiring evidence-driven, compliance-aware decisions.
- Expects project documentation to exist in-repo (including common text and office/PDF formats).
- Requires explicit uncertainty labeling when evidence is missing.

## Acknowledgments

This project is inspired by the `forge-council` repository by N4M3Z. It was brought to life using `forge-core` skills BuildAgent and BuildSkill.....
